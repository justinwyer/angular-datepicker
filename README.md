A date picker for angularjs

 * It's well tested
 * Depends on momentjs
 * Code + template < 130 lines of code :)

Create an issue or send a pull request!

Setup the environment:
```shell
$ npm install
$ bower install
```
Development:
```shell
$ npm test # runs karma
$ grunt example # launches the example
```